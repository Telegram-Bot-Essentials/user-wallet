name: stale

on:
  schedule:
    - cron: '0 4 * * 1'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: Telegram-Bot-Essentials/.github/.github/workflows/stale.yml@main
